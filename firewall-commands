# Enable UFW firewall
sudo ufw enable

# Check firewall status and existing rules
sudo ufw status verbose

# Block Telnet (port 23)
sudo ufw deny 23

# Test Telnet connection
telnet localhost 23

# Allow SSH (port 22)
sudo ufw allow 22

# Remove Telnet block rule (restore original state)
sudo ufw delete deny 23
